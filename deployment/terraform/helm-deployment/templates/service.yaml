apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-cloud.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "test-cloud.name" . }}
    helm.sh/chart: {{ include "test-cloud.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "test-cloud.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
